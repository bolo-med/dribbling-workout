import { SekundeFormatPipe } from './sekunde-format.pipe';

describe('SekundeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SekundeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
